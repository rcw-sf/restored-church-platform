{
  "name": "@repo/rules",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsx scripts/build-rules.ts",
    "typecheck": "tsc -b --noEmit",
    "test": "pnpm build && firebase emulators:exec --project demo-test --only firestore --config ../../firebase.json 'vitest run'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "@types/node": "25.9.2",
    "firebase": "12.14.0",
    "typescript": "6.0.3",
    "vitest": "4.1.8"
  },
  "dependencies": {
    "tsx": "4.22.4"
  }
}
